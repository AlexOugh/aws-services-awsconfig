version: 0.1
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package
         --template-file template.basic.yaml
         --s3-bucket sgas.particles-awsconfig.089476987273.us-east-1
         --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files:
    - NewSamTemplate.yaml
